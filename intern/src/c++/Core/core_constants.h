#pragma once

#include "core_types.h"

namespace Core
{
    const Core::UInt IMAGE_SIZE = 4;
    const Core::F32 HEIGHT_CONVERSION = 1.0f;  ///< The factor to convert a height value of the height-map to the actual height
}